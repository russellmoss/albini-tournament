# Design: First Inaugural Albini Invitational (Juergens Memorial Caroms Tournament)

Generated by /office-hours on 2026-04-18
Branch: master
Repo: russellmoss/albini-tournament (local; not yet on GitHub at design time)
Status: DRAFT
Mode: Builder

## Problem Statement

Run a one-night, in-person caroms billiards tournament for ~15 friends in a "World Cup" format: group stage → 8-player single-elimination knockout. The host runs everything from a phone or laptop on the night. Spectators watch the bracket on their own phones. The visual identity is Steve Albini — groups named after records he produced, blurred album-cover slideshow behind a framed content card.

Goal for night-of: zero arguing about scores, zero confusion about who plays who next, looks great on a phone in a dim bar.

## What Makes This Cool

- The Steve Albini conceit. Group "In Utero" plays Group "Surfer Rosa" feels like the right tribute.
- The album-cover slideshow gives the app a distinct atmosphere — most tournament apps look like spreadsheets.
- The "First Inaugural" framing implies a tradition. If this works, it runs again next year.
- Caroms tournaments rarely get a real bracket UI. This is a small group of friends getting a presentation that feels like a real event.

## Constraints

- ~15 players, exact count known by lock time, no late arrivals after lock.
- Stack fixed: Next.js 15 (App Router), Neon Postgres, Vercel deploy.
- ENV already has Anthropic + Gemini keys (not used in MVP, but available for future features like AI-generated match commentary).
- Album cover images live at `C:\Users\russe\Documents\albini-tournament\images` (currently 3 files; user will collect 20–30 before launch and pre-blur them at build time).
- Mobile-first: host runs admin from a phone, spectators view bracket on phones.
- One-evening event: no need for offline mode, persistent sessions across days, or scaling beyond ~30 concurrent viewers.

## Premises

Confirmed during /office-hours session:

1. **N=15, 3 groups of 5.** 6 group winners (top 2 each) + 2 wildcards = 8 in knockout. "Wildcard" = best-of-rest selected by the same tiebreaker waterfall used inside groups.
2. **Hard registration lock.** State machine: register → lock → draw groups → group play → knockout → done. No late arrivals after lock. If someone flakes after the draw, their group plays with a forfeit — too bad.
3. **Tiebreaker waterfall** (within group AND for wildcard selection):
   1. Games won
   2. Total points scored across group games
   3. Point differential between the tied players (head-to-head)
   4. If 3+ players are tied: re-apply 1–3 to the mini-table among only the tied players
   5. If still tied: random (documented coin flip in the UI)
4. **Per-game normalization is required for wildcard math** when groups are different sizes. With N=15, 3×5, all groups are equal so this doesn't bite — but the algorithm should compute win rate / points-per-game / diff-per-game so it stays correct if N drifts in future tournaments.
5. **Strict seed bracket.** 1v8, 4v5, 3v6, 2v7. Top half: (1v8) vs (4v5). Bottom half: (3v6) vs (2v7). Final winners of top vs bottom. No World Cup–style group-cross seeding. Group-stage rematches in QF are fine.
6. **Multi-tournament schema from day one.** The data model assumes a `tournaments` table; the UI hardcodes "the active tournament" for night-of simplicity.
7. **Auth: admin PIN cookie.** One shared 4-digit PIN unlocks write actions. Public read-only bracket. No NextAuth, no magic links.
8. **Album slideshow: pre-blurred WebPs at build time** (user is collecting 20–30 covers before launch). Dark overlay (`rgba(0,0,0,0.5)`) under the framed content card for text readability.
9. **Polling, not SSE.** Bracket page polls server every 5s. Acceptable lag for one-night event.

## Approaches Considered

### Approach A: Server Actions + Polling (Recommended)
Normalized 6-table schema. Next.js 15 RSC pages, server actions for every mutation. 5s polling on the spectator bracket page. Drizzle ORM. PIN cookie middleware for admin routes. ~12 hours to build.

### Approach B: Server Actions + SSE
Same as A but the bracket page subscribes to a Server-Sent Events route handler. Live push on every match result insert. Requires Upstash Redis pub/sub channel for serverless-safe broadcast. ~16 hours. Better in-room feel; more moving parts and more risk on Vercel free-tier function-duration limits.

### Approach C: Tournament-as-Document
2-table schema with all tournament state in a single `state jsonb` column. Server actions are pure functions over the blob. ~6 hours. Fastest to build, but cross-tournament queries become awkward and the eventual port to normalized rows is a real migration.

## Recommended Approach: A — Server Actions + Polling

### Architecture

```
Next.js 15 (App Router, RSC)
  ├─ app/
  │   ├─ (public)/
  │   │   ├─ page.tsx              ← landing / current tournament status
  │   │   ├─ groups/page.tsx       ← live group standings (5s poll)
  │   │   └─ bracket/page.tsx      ← live knockout bracket (5s poll)
  │   ├─ admin/
  │   │   ├─ login/page.tsx        ← PIN entry
  │   │   ├─ page.tsx              ← admin dashboard
  │   │   ├─ players/page.tsx      ← register / lock
  │   │   ├─ groups/page.tsx       ← post group match results
  │   │   └─ knockout/page.tsx     ← post knockout match results
  │   ├─ api/                      ← (none needed; server actions only)
  │   └─ middleware.ts             ← PIN cookie gate for /admin/*
  ├─ lib/
  │   ├─ db.ts                     ← Drizzle + Neon client
  │   ├─ schema.ts                 ← Drizzle schema
  │   ├─ tournament/
  │   │   ├─ draw.ts               ← group assignment algorithm
  │   │   ├─ standings.ts          ← compute standings + tiebreakers
  │   │   ├─ wildcards.ts          ← best-of-rest selection
  │   │   └─ bracket.ts            ← seed → bracket slot mapping
  │   └─ auth.ts                   ← PIN cookie helpers
  ├─ components/
  │   ├─ Slideshow.tsx             ← background album-cover cycler
  │   ├─ FramedCard.tsx            ← content frame w/ overlay
  │   ├─ GroupTable.tsx
  │   └─ Bracket.tsx
  └─ public/
      └─ covers/                   ← 20–30 pre-blurred WebPs
          ├─ in-utero-blur.webp
          ├─ surfer-rosa-blur.webp
          └─ ...
```

### Schema (Drizzle)

```typescript
// 7 tables. Multi-tournament from day one.

tournaments {
  id          uuid pk
  name        text not null               // "First Inaugural Albini Invitational"
  status      text not null               // 'draft' | 'locked' | 'group_play' | 'knockout' | 'done'
  config      jsonb not null              // { group_count: 3, group_size: 5, advance_top: 2, wildcard_count: 2 }
  created_at  timestamptz default now()
  updated_at  timestamptz default now()
}

players {
  id              uuid pk
  tournament_id   uuid fk → tournaments.id (on delete cascade)
  name            text not null
  registered_at   timestamptz default now()
  withdrawn_at    timestamptz             // nullable; set when admin marks player as withdrawn
  unique(tournament_id, name)             // no two "Steves" in one tournament
}

groups {
  id              uuid pk
  tournament_id   uuid fk → tournaments.id
  name            text not null           // "In Utero", "Surfer Rosa", "At Action Park"
  position        smallint not null       // 1, 2, 3 — for stable display order
  unique(tournament_id, position)
}

group_assignments {
  id          uuid pk
  group_id    uuid fk → groups.id (on delete cascade)
  player_id   uuid fk → players.id (on delete cascade)
  unique(group_id, player_id)
  unique(player_id)                       // a player is in exactly one group per tournament
}

matches {
  id                       uuid pk
  tournament_id            uuid fk → tournaments.id
  phase                    text not null           // 'group' | 'qf' | 'sf' | 'final'
  group_id                 uuid fk → groups.id     // nullable; only for phase='group'
  bracket_slot             smallint                // nullable; 1..7 for knockout (1-4 = QF, 5-6 = SF, 7 = Final)
  player_a_id              uuid fk → players.id    // nullable: NULL allowed when phase IN ('sf','final') AND status='pending'
  player_b_id              uuid fk → players.id    // nullable: same rule
  status                   text not null           // 'pending' | 'done'
  went_to_sudden_death     boolean not null default false   // displayed as a badge on bracket/standings
  played_at                timestamptz
  check (
    (phase = 'group' AND group_id IS NOT NULL AND bracket_slot IS NULL
       AND player_a_id IS NOT NULL AND player_b_id IS NOT NULL)
    OR (phase != 'group' AND group_id IS NULL AND bracket_slot IS NOT NULL
       AND (status = 'done' IMPLIES (player_a_id IS NOT NULL AND player_b_id IS NOT NULL)))
  )
}

match_results {
  id          uuid pk
  match_id    uuid fk → matches.id (on delete cascade)
  player_id   uuid fk → players.id
  points      smallint not null           // raw carom total across all innings (incl. sudden death); 0 for forfeit
  won         boolean not null            // true for the winner; ties resolved via sudden death so always exactly one winner per match
  forfeit     boolean not null default false  // true on the withdrawn player's row; opponent's row stays false
  unique(match_id, player_id)             // one row per player per match (always exactly 2)
}

auth_attempts {
  id           uuid pk
  ip           text not null              // request IP from x-forwarded-for; ipv4 or ipv6 string
  attempt_at   timestamptz not null default now()
  succeeded    boolean not null default false
  index        (ip, attempt_at)           // server action queries WHERE ip=$1 AND attempt_at > now() - interval '10 minutes'
}
```

**Indexes**: `(tournament_id, phase)` on matches, `(tournament_id)` on players, `(ip, attempt_at)` on auth_attempts. The dataset is tiny (~50 rows total per tournament) so additional indexes are unnecessary.

### State Machine

```
draft ─[admin: Lock Registration]─→ locked
locked ─[admin: Draw Groups]──────→ group_play
group_play ─[all group matches done]
           AND [admin: Start Knockout]─→ knockout
knockout ─[final played]──────────→ done
```

The `status` column gates which UI surfaces are reachable. Spectator pages render different content per state ("Registration open — N players signed up" vs. "Group play in progress" vs. "Knockout: round of 8" vs. "Champion: <name>").

### Algorithms

**1. Group draw** (`lib/tournament/draw.ts`)
- Input: array of player IDs, group_count from `tournaments.config`
- Shuffle with crypto-grade RNG (`crypto.getRandomValues` over a Fisher-Yates pass)
- Distribute players round-robin into groups: player[i] → group[i % group_count]
- Generate group names: pick `group_count` unique names from a curated Albini-records list (`['In Utero', 'Surfer Rosa', 'At Action Park', 'Rid of Me', 'Pod', 'Atomizer', 'The Hammer Party', 'Songs About Fucking', ...]`) — shuffle and take first N
- Generate group matches: round-robin within each group. For 5-player groups, that's `C(5,2) = 10` matches per group, 30 total
- All matches start with `status='pending'`; admin clicks each as it's played

**2. Standings + tiebreakers** (`lib/tournament/standings.ts`)
- Compute per-player within a group: `games_played`, `wins`, `points_scored`, `point_diff`
- Sort by waterfall: `wins DESC`, `points_scored DESC`, then resolve ties:
  - For 2-way ties: `head_to_head_diff DESC` (the diff in their direct match)
  - For 3+-way ties: build a sub-table containing only matches between tied players, re-apply `wins → points_scored → diff` over the sub-table
  - If still tied: deterministic random (seeded by tournament_id + player_ids sorted) — same seed always produces the same order, so the UI can show "tied; coin flip resolved in favor of X" without arguing

**3. Wildcard selection** (`lib/tournament/wildcards.ts`)
- After all group matches done, collect all non-top-2 finishers across groups (for N=15: 3 groups × 3 non-top-2 = 9 candidates)
- Compute per-game metrics: `wins / games_played`, `points_scored / games_played`, `point_diff / games_played`
- Sort candidates by per-game waterfall: `win_rate DESC`, `pts_per_game DESC`, `diff_per_game DESC`
- Pick top `wildcard_count` (2 for N=15)
- Deterministic random tiebreak as above if still tied at the cutoff

**4. Bracket seeding** (`lib/tournament/bracket.ts`)
- Combine 6 group winners (top 2 from each group) + 2 wildcards = 8 players
- Assign global seeds 1–8 by overall standing across all 8: `wins DESC`, `pts_per_game DESC`, `diff_per_game DESC`, then deterministic random
- Place into bracket slots:
  - QF1: seed 1 vs seed 8 (slot 1)
  - QF2: seed 4 vs seed 5 (slot 2)
  - QF3: seed 3 vs seed 6 (slot 3)
  - QF4: seed 2 vs seed 7 (slot 4)
  - SF1 (slot 5): winner QF1 vs winner QF2
  - SF2 (slot 6): winner QF3 vs winner QF4
  - Final (slot 7): winner SF1 vs winner SF2
- Insert all 7 matches at once with `status='pending'` and `player_a_id`/`player_b_id` populated for QF; SF and Final get populated as upstream matches complete (server action that posts a match result also updates downstream pending matches)

### UI Surfaces

**Public**
- `/` — landing card. Shows tournament name, status, player count, and a "Watch Bracket" CTA once group play starts.
- `/groups` — live group standings. Three tables (one per group), highlighting top-2 in green and 3rd-place candidates in amber while group play is active. Polls every 5s.
- `/bracket` — knockout bracket. SVG/CSS rendering of the 8-player bracket. Polls every 5s. Champion celebration screen on `done`.

**Admin** (gated by PIN cookie middleware)
- `/admin/login` — PIN entry. POSTs to a server action that sets a signed cookie.
- `/admin` — dashboard. Big buttons that change with state: "Add Player" / "Lock Registration" / "Draw Groups" / "Post Group Result" / "Start Knockout" / "Post Knockout Result".
- `/admin/players` — add/remove players (only when status='draft'); list of registered players.
- `/admin/groups` — list of pending group matches; tap one → score entry (two number inputs + a winner radio); submit → server action.
- `/admin/knockout` — same UI shape as group match entry, but for QF/SF/Final.

### Auth (PIN cookie)

- Admin sets `ADMIN_PIN` env var on Vercel (e.g., `4729`). Also sets `ADMIN_RECOVERY_PIN` (longer, never rate-limited) for self-recovery if locked out by attacker spam.
- `/admin/login` form posts PIN to a server action.
  - First, look up `auth_attempts` for the request IP in last 10 min. If >=5 (and the PIN being submitted is not the recovery PIN), reject without checking PIN. Insert a row with `succeeded=false`.
  - If PIN matches `ADMIN_PIN` or `ADMIN_RECOVERY_PIN`: insert row with `succeeded=true`, set a signed cookie.
  - If PIN doesn't match: insert row with `succeeded=false`, return error.
- Cookie attributes: `HttpOnly`, `Secure`, `SameSite=Lax`, `Path=/admin`, `Max-Age=43200` (12h). Signed with `AUTH_SECRET` env var via `iron-session` or `jose`.
- `middleware.ts` checks the cookie on every `/admin/*` route; redirects to `/admin/login` if missing/invalid.
- No user accounts. Public pages have no auth.

### Background Slideshow

- 20–30 album covers pre-processed at build time (or pre-deploy):
  - Convert to WebP, max 1920×1920
  - Apply blur in image processing (`sharp({...}).blur(40)`) — bake the blur in, don't rely on CSS `filter: blur()` (CPU-expensive, janky on phones)
  - Output to `public/covers/*-blur.webp`
- Client component `Slideshow.tsx`:
  - Renders a fixed-position container behind everything
  - Cross-fades between two `<img>` elements every 8 seconds
  - Shuffles cover order on mount
  - Preloads next image via `<link rel="preload" as="image">` injected at runtime
  - `onerror` handler skips a missing image without breaking the cycle
  - Respects `prefers-reduced-motion`: freezes on the first cover, no cross-fade
- `FramedCard.tsx` wraps page content with:
  - `rgba(0,0,0,0.55)` darkening overlay layer (raised from 0.5 — needed for WCAG AA contrast over moving blur)
  - Centered card with sharp 0px corners, 1px solid `--fg` border, `--bg-card` background, `--fg` text
  - All page content lives inside `<FramedCard>` so the overlay is consistent

### Visual Language (design tokens)

```
COLORS
  --bg              #0a0a0a       near-black, page background
  --bg-card         #000000       framed-card background
  --fg              #f5f3ee       primary text (off-white, paper-like)
  --fg-muted        #8a8a8a       eliminated rows, secondary metadata
  --accent          #1f6b3a       caroms-table green (top-2 edge, winner, focus, sudden-death outline)
  --overlay         rgba(0,0,0,0.55)

TYPOGRAPHY
  Font: JetBrains Mono (self-hosted via @next/font), all surfaces, all weights
  Scale: 12 / 14 / 16 / 20 / 28 / 40 / 64 px
  Weights: 400 (body), 600 (group names, scores), 800 (tournament title, champion name)

SPACING (8pt grid)
  4 / 8 / 12 / 16 / 24 / 32 / 48 / 64
  Page gutter: 16px @ <640px, 32px @ 640-1024px, 64px @ >1024px

BORDER
  Default: 1px solid var(--fg-muted)
  Card: 1px solid var(--fg)
  Radius: 0px on FramedCard; 4px max anywhere else; NEVER Tailwind's rounded-lg/xl

SHADOWS
  none. ever. anywhere.

MOTION
  UI duration: 200ms, cubic-bezier(0.4, 0, 0.2, 1)
  Slideshow cross-fade: 8000ms cycle, 800ms transition
  Reveal animations (group draw, knockout start): 100ms stagger, 1s total, runs ONCE
    (state-machine-driven; not on every render)
  Respects prefers-reduced-motion: cuts cross-fade, removes reveal stagger

VISUAL VOCABULARY
  Top-2 in group:    bold + 2px green left edge stroke + "Advances" subtitle text
                     (NEVER colored circle, NEVER full background fill, NEVER card)
  3rd-place candidates: standard weight, no edge
  Eliminated:         --fg-muted color + "Out" subtitle
  Winner of match:    bold + green "W" badge + score in bold green
  Sudden death:       small-caps "SUDDEN DEATH" in 1px outlined box (not a pill, not a circle)
  Up Next card:       2px solid --accent border, sticky top of viewport on public pages
  Focus rings:        2px solid --accent, 2px offset

NO-FLY ZONES (anti AI-slop guardrails)
  - No purple/violet/indigo (Tailwind defaults). Only the palette above.
  - No bubbly border-radius. Sharp corners default.
  - No icons-in-colored-circles for state.
  - No emoji as design elements.
  - No 3-column feature grids anywhere.
  - No decorative shadows, blobs, gradients, or wavy SVG dividers.
  - No "Welcome to..." copy. Tournament name does the work.
```

### Information Architecture

```
PAGE          | ANCHOR              | SECONDARY               | TERTIARY
/             | Tournament name 64pt| Status banner           | "Up Next" sticky + CTA
/groups       | "Up Next" sticky    | 3 group tables          | per-row "Advances/Out" tag
/bracket      | "Up Next" sticky    | Bracket (vertical mob.) | Completed match log below
/admin        | One PRIMARY button  | Secondary actions       | Status indicator
/admin/players| Player roster       | Add-player input        | Lock button (when ≥2)
/admin/groups | Pending matches list| Score-entry form        | Quick "skip to next group"
/admin/knockout | Current round    | Score-entry form        | Bracket preview
/admin/login  | PIN keypad          | Error message inline    | "Recovery PIN?" link
```

### Interaction State Coverage

```
PAGE              | EMPTY                  | LOADING            | ERROR              | SUCCESS
/                 | "Registration open ·   | skeleton banner    | banner: error      | banner: status
                  |  0 of 15"              |                    |                    |  updated
/groups           | tables w/ "—" + "0     | skeleton tables    | "Connection lost,  | partial: amber
                  |  played" tag           |                    |  retrying"         |  on candidates
/bracket          | knockout: bracket w/ ? | skeleton bracket   | same               | winner: green W
                  | group_play: copy + link|                    |                    |
/admin/players    | "Add first player"     | none (instant)     | inline form err    | clear+focus next
/admin/groups     | "All matches done.     | skeleton list      | inline form err    | toast + load next
                  |  Start Knockout?"      |                    |                    |  pending match
/admin/knockout   | "All matches done.     | skeleton list      | inline form err    | toast + advance
                  |  See champion."        |                    |                    |
/admin/login      | n/a                    | none               | red text under PIN | redirect
```

Loading uses `<Suspense fallback={<Skeleton/>}>` around polled regions. Skeleton matches expected row count, neutral pulse, no spinners anywhere.

Score validation: server action enforces `0 ≤ points ≤ 200`, integer. Client shows inline error before submit if input is empty/invalid/out-of-range.

### User Journey & Emotional Arc

```
HOST JOURNEY
STEP | DOES                       | FEELS                    | UI SUPPORTS
1    | opens app, dim bar         | excited, tribute mood    | slideshow + framed card
2    | adds players as they show  | rushed, social           | 44px+ touch targets, tap-add
3    | locks registration         | committed                | confirm modal: "no more late arrivals"
4    | taps Draw Groups           | reveal moment            | 1s sequential reveal, once only
5    | posts each group result    | satisfaction             | success toast + auto-load next
6    | starts knockout            | tension                  | reveal animation, ceremony
7    | posts QF/SF/Final          | mounting drama           | bracket fills visibly
8    | sees champion screen       | celebration              | dedicated screen, 64pt name

SPECTATOR JOURNEY
STEP | DOES                       | FEELS                    | UI SUPPORTS
1    | opens shared URL           | curious                  | landing card
2    | sees blurred album         | "they made an APP"       | first 5 sec are visceral
3    | finds their name in group  | belonging                | name visible, group named
4    | watches "Up Next"          | anticipation             | sticky card, glance-friendly
5    | bracket appears post-groups| "we're playing for real" | reveal moment
6    | refreshes during matches   | shared tension           | live updates
7    | celebrates winner          | shared moment            | champion screen
```

Time-horizon design:
- **5 sec (visceral)**: blurred album cover slideshow + framed card with tournament name = unmistakable identity. No corporate web-app feel.
- **5 min (behavioral)**: every interaction (registration, score posting, bracket viewing) is fast and tactile.
- **5 year (reflective)**: data persists; the bracket from this year becomes part of the tradition. Champion name lives in Postgres forever.

### Responsive & Accessibility

```
BREAKPOINTS (Tailwind defaults)
  <640px   phone     | single column, 16px gutter, vertical stacked bracket
  640-1024 tablet    | two-column where applicable, 32px gutter, horizontal bracket
  >1024px  desktop   | three-column groups, 64px gutter, full horizontal bracket

A11Y BASELINE
  - All interactive elements as <button> or <a>, never clickable <div>.
  - Visible focus rings: 2px solid --accent, 2px offset.
  - aria-live="polite" on bracket and groups regions for poll-driven updates.
  - aria-live="polite" on Up Next card.
  - Form inputs: real <label htmlFor>, never placeholder-as-label.
  - Color contrast: WCAG AA. --fg over --bg = 16:1. --fg over (slideshow + --overlay)
    must be tested at build time across all 20-30 covers; bump --overlay alpha if any fails.
  - State NEVER color-only: top-2 = bold + green edge + "Advances" text;
    eliminated = muted color + "Out" text; winner = bold + green W badge.
  - Touch targets ≥ 44x44px (iOS HIG).
  - prefers-reduced-motion: slideshow freezes; reveal animations disabled.

BRACKET MOBILE STRATEGY
  <1024px: Vertical stack — QF1, QF2, QF3, QF4, SF1, SF2, Final, top to bottom.
           Each match = full-width row: player A name | "vs" | player B name | score | status badge.
           Reads top-down, scrolls naturally, no pinch-zoom needed.
  ≥1024px: Traditional horizontal bracket, classic shape.
```

### Champion Screen

When `tournament.status='done'`:

```
                    [framed card]

         FIRST INAUGURAL ALBINI INVITATIONAL
                       CHAMPION

                    {NAME 64pt 800}

         ──────────────────────────────────

         Path to victory:

           Group play: 4–0 in {group name}
           QF: d. Tom 18–11
           SF: d. Mike 22–19 (SUDDEN DEATH)
           Final: d. Russell 18–14

                    [framed card end]
```

JetBrains Mono throughout. Champion name in 64pt 800 weight. Path-to-victory rendered as one row per match in 16pt 400.

### Polling

- All public pages use a client component `<PollingRefresh interval={5000} />` that triggers `router.refresh()` on a timer
- `router.refresh()` re-runs RSC; minimal payload because RSC streams diffs
- No additional libraries. Stop polling when document is hidden (`document.hidden` check in the poll callback) to save battery
- Also fire one immediate `router.refresh()` on `visibilitychange` when becoming visible — handles "phone unlocked, page is now stale"

### Concurrency & State Transitions

- All state-changing server actions use **optimistic locking** via `UPDATE ... WHERE status = $expected RETURNING id`. If 0 rows return, the action throws "state changed under you, please refresh."
- Why optimistic (not `SELECT FOR UPDATE`): the chosen `neon-http` driver doesn't hold cross-statement transactions. `UPDATE WHERE` gives the same correctness guarantee for single-row state machines and works inside neon-http's batched transactions.
- All multi-row inserts (group draw, knockout seeding) run as a single batched transaction via `db.transaction([q1, q2, ...])`. Either all rows insert or none do.

### Deterministic RNG (`lib/tournament/random.ts`)

- Tiebreakers and bracket-tie resolution must be reproducible — same tournament + same player set always produces the same order, so the UI can display "tied; coin flip resolved in favor of X" without anyone arguing.
- Use **mulberry32**, a 6-line PRNG. No dependency.
- Seed with FNV-1a hash of `tournament_id + sortedPlayerIds.join(',')`.
- Same seed → same shuffle. Tested in `random.test.ts` with snapshot of 1000-iteration outputs.

### "Up next" Card

- Added to top of `/groups`, `/bracket`, and `/`.
- Reads `matches WHERE status='pending' ORDER BY phase, bracket_slot NULLS LAST, id` LIMIT 1.
- Displays "Up next: {player_a.name} vs {player_b.name}".
- During group play, `phase='group'` matches don't have a defined order — first by insertion is fine; the host can mentally resequence.
- Polls with the rest of the page (5s).

## Resolved Decisions

1. **Album cover licensing.** Deploy on the default `albini-tournament.vercel.app` subdomain. No mitigation needed — this is a 15-person private event and fair use covers it in practice.
2. **Early withdrawal.** Admin can mark a player as "withdrawn" before any of their pending matches are posted. Remaining matches are forfeited: each forfeited match inserts two `match_results` rows with `points=0`; the present opponent gets `won=true` and the withdrawn player gets `won=false`. Forfeits **do** count toward the opponent's `games_won` in the tiebreaker waterfall (a forfeit win is a win).
3. **Caroms scoring — match format.** Each match is **9 innings per player**. An inning is one player's turn at the table — they continue shooting as long as each shot scores a carom (1 point per carom). A missed shot ends the inning. Match winner = more total carom points across 9 innings. If tied after 9 innings, **sudden death**: one additional inning each, repeat until the tie breaks. Score-entry UI: two number inputs (final carom totals for each player); server computes winner and updates `games_won` in group standings. `match_results` schema unchanged — `points` stores the carom total, `won` is boolean. Sudden-death innings are aggregated into the same totals (the spec doesn't separate them; the boxscore is just the final number).
4. **AI commentary / "Champion's Story".** Deferred to post-MVP. The `match_results` table is already a complete event log, so this can be added later without schema changes.
5. **Domain.** Default Vercel subdomain `albini-tournament.vercel.app`. No custom domain.

## Success Criteria

- Host can register all 15 players and lock in under 5 minutes from a phone in a dim bar.
- Group draw is one tap; group match results are 3 taps each (open match → enter scores → submit).
- Spectators can watch live bracket updates on their phones with no install, no auth.
- Zero server errors during the event.
- The slideshow renders smoothly on a 3-year-old iPhone (no jank, no battery panic).
- The bracket page loads in under 1 second on 4G.
- Tomorrow morning, the data is still in Postgres for next year's "Second Annual."

## Distribution Plan

- **Hosting**: Vercel (already chosen). Push to main → auto-deploy.
- **Database**: Neon Postgres free tier (10 connections, plenty for one-night event). Connection string in `DATABASE_URL` env var.
- **Image assets**: pre-blurred WebPs committed to repo at `public/covers/`. (Consider `.gitattributes` LFS only if total cover-art size exceeds ~10MB.)
- **Cover prep script**: `scripts/prep-covers.ts` runs `sharp` over `images/` → outputs to `public/covers/`. Wired as `prebuild` in `package.json` so it runs automatically on every Vercel deploy. Idempotent: skips files where the WebP output is newer than the source.
- **No CI required for MVP** — Vercel preview deploys per-PR are sufficient.

## Dependencies

- **Runtime**: Drizzle ORM + Drizzle Kit (migrations); `drizzle-orm/neon-http` driver (no persistent connections, simplest in serverless); `iron-session` (PIN cookie signing); Tailwind CSS.
- **Build-time**: `sharp` (cover blur, dev dep).
- **Test**: Vitest + `@testing-library/react` (unit/component); Playwright (E2E happy path + spectator live-update + withdrawal flow).
- **Test coverage targets**: `lib/tournament/*` = 100% line + branch (the algorithmic core); components = smoke; E2E = 3 critical flows.

## Next Steps (Build Order)

0.5. **Test scaffolding**: install Vitest + Playwright, wire up `npm test` and `npm run e2e`, add CI-ready config. (Tests get written alongside each step below.)
1. **Bootstrap**: Next.js 15 + Drizzle + neon-http connected; `tournaments` table seeded with one row.
2. **Player registration UI** (admin, public landing) — register → see new player appear. Includes withdrawal action (sets `players.withdrawn_at`).
3. **Lock + group draw**: implement `draw.ts` + `random.ts`, the "Lock Registration" and "Draw Groups" server actions with optimistic locking, render group tables on `/groups`.
4. **Group match entry + standings**: score entry UI (with sudden-death checkbox) + `standings.ts` + tiebreaker waterfall. Match-edit UX (TODO 1) included. Write unit tests against synthetic group results — 100% coverage on this module.
5. **Wildcard selection + knockout seeding**: implement `wildcards.ts` and `bracket.ts`. Insert QF matches (with player_a/b populated) and SF/Final shells (with NULL player_a/b) via "Start Knockout" server action.
6. **Knockout match entry + bracket rendering**: same shape as group entry. Bracket SVG layout. `advanceWinner` server action populates downstream slot's player_a or player_b in the same TX as the result insert.
7. **PIN cookie auth + middleware + lockout**: gate `/admin/*`. Implement `auth_attempts` rate limit. Recovery PIN escape hatch. **Independent of steps 2-6 — can run in parallel worktree.**
8. **Slideshow + framed card**: `prep-covers.ts` (wired as prebuild), `Slideshow.tsx` with preload + onerror skip + prefers-reduced-motion freeze, `FramedCard.tsx`. **Includes `scripts/check-contrast.ts`** that samples each pre-blurred WebP at 9 grid points, computes WCAG contrast of `#f5f3ee` over (sample × `--overlay`), fails build if any cover drops below 4.5:1. **Independent of steps 2-6 — can run in parallel worktree.**
9. **"Up next" card** (TODO 2): top-of-page reads next pending match.
10. **Polish**: champion celebration screen, mobile spacing, dark mode default, network-failure toast on form submits.
11. **Deploy**: Vercel default subdomain. Set `ADMIN_PIN`, `ADMIN_RECOVERY_PIN`, `AUTH_SECRET`, `DATABASE_URL` env vars.

### Worktree parallelization

After step 1 lands, spawn three lanes:

```
Lane A (sequential, shared lib/tournament/ + admin/):
  2 → 3 → 4 → 5 → 6 → 9
Lane B (independent — auth-only): 7
Lane C (independent — visual-only): 8
After A + B + C: 10 → 11
```

Lane A and Lane B both touch `schema.ts`. Land Lane B's `auth_attempts` migration first, then Lane A migrates onto it.

## What I noticed about how you think

- You corrected the "+1 wildcard" math in your own brief without prompting and accepted "best-of-rest by the same waterfall" as the cleaner rule. That's the kind of self-edit that catches design bugs before they hit code.
- You drew a hard line on "no late arrivals, ever" instead of asking for a workaround. Most people ask for the workaround and then build it and then never use it. You skipped both steps.
- You corrected my over-reading of "World Cup format" — you meant the structure (groups → knockout), not the cross-group seeding rules. Concrete > prestigious. That's the right priority for a one-night build.
- You called out the slideshow image gap as an action item rather than a blocker. Pragmatic.

---

## Design Review Decisions (2026-04-18)

Folded in from `/plan-design-review` session:

- **Visual language section** added with full design tokens (colors, type, spacing, motion, anti-AI-slop guardrails).
- **Font**: JetBrains Mono everywhere, self-hosted via `@next/font`.
- **Accent color**: caroms-table green `#1f6b3a` (top-2 edge, winner, focus rings, sudden-death outline, Up Next border).
- **No shadows. Sharp 0px corners on the framed card. 4px max anywhere else.**
- **Information Architecture** table added: each page's primary/secondary/tertiary anchor.
- **Up Next card**: sticky top of viewport on every public page; 2px green border.
- **Interaction state coverage** table added: empty/loading/error/success per page. Skeletons via `<Suspense>`, no spinners.
- **Score validation**: `0 ≤ points ≤ 200`, integer; inline errors before submit.
- **Score-success behavior**: form clears, success toast, auto-load next pending match (admin speed-of-entry matters).
- **User journey** storyboards added (host + spectator) with emotional arc per step.
- **Reveal animations**: 1s sequential reveal on group draw and knockout start, runs ONCE (state-machine-driven, not per-render).
- **Confirm modal** on Lock Registration ("no more late arrivals").
- **Responsive breakpoints**: phone <640px / tablet 640-1024 / desktop >1024px.
- **Mobile bracket strategy**: vertical stacked matches (QF1...Final top-down) under 1024px; traditional horizontal at desktop.
- **A11y baseline**: keyboard nav, visible focus, aria-live on poll regions, real labels, WCAG AA contrast (verified against slideshow + overlay at build time), state never color-only, 44px touch targets, prefers-reduced-motion respected.
- **Champion screen**: 64pt champion name + path-to-victory match log, framed card, JetBrains Mono.

## Eng Review Decisions (2026-04-18)

Folded in from `/plan-eng-review` session:

- **Schema**: 7 tables (added `auth_attempts` for PIN brute-force protection; `withdrawn_at` on `players`; `forfeit` and `went_to_sudden_death` columns).
- **Driver**: `drizzle-orm/neon-http` (HTTP, no persistent connections, simplest in serverless).
- **State guards**: optimistic locking via `UPDATE ... WHERE status=$expected`. Concurrent admin tabs get "state changed under you, refresh" instead of clobbering.
- **PIN security**: rate limit (5 attempts / 10 min / IP) + recovery PIN escape hatch + cookie flags `HttpOnly, Secure, SameSite=Lax, Path=/admin`.
- **Cover prep**: wired as `prebuild` script, idempotent (skip if WebP newer than source).
- **Tests**: Vitest + Playwright. 100% on `lib/tournament/*`. 3 E2E flows: admin happy path, spectator live update, withdrawal flow.
- **TODOs added to this PR scope**: match-edit UX (Step 4), "Up next" card (Step 9).
- **TODOs deferred**: pre-flight sanity check page (skipped), AI commentary (Resolved Decision #4), custom domain (Resolved Decision #5).

## Unresolved Decisions

- **None blocking.** All architecture / quality / test / performance decisions resolved during the review. Match-edit UX scope is "edit allowed until tournament status advances past that match's phase" — refine during implementation if friction shows up.

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 0 | — | — |
| Codex Review | `/codex review` | Independent 2nd opinion | 0 | — | — |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 1 | CLEAR | 13 issues, 0 critical gaps unresolved (4 test-coverage gaps added to test plan) |
| Design Review | `/plan-design-review` | UI/UX gaps | 1 | CLEAR | initial 4/10 → final 8.5/10, 9 design decisions added |
| DX Review | `/plan-devex-review` | Developer experience gaps | 0 | — | — |

**UNRESOLVED:** 0
**VERDICT:** ENG + DESIGN CLEARED — ready to implement.

**Status: APPROVED — ready for implementation.**
